# Internal / never publish
RETRIEVED/

# Build artifacts / archives (delivered as release assets, not tracked)
*.tar.gz
*.tar.gz.sha256
SHA256SUMS

# Env & secrets
.env
.env.*

# Python
__pycache__/
*.pyc
.DS_Store
node_modules/
.pytest_cache/
outputs/
*.safetensors
*.gguf

# Keep other direction artifacts private. Only the explicitly published
# Qwen Image inference bank and its manifest are allowed below.
server/data/*.npy
server/data/*.json
!server/data/qwen-image21-dwm.safetensors
!server/data/qwen-image21-dwm.json

# ComfyUI node vectors — delivered to licensed customers, not public
comfyui/data/*.npy
comfyui/data/*.json

# Optional MCP virtual environment
.venv-mcp/
